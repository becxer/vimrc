##becxer's vimrc & plugins

* include modules

    - plugins(jedi,NERDTree,tag-navigator,empty-template)
    - vifm(vim style file manager)
    - ctags
    - .vimrc
    - .ctags


        $./install-vim.sh
